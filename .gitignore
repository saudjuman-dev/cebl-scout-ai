# Node dependencies — installed fresh by GitHub Actions / locally as needed
node_modules/

# Local environment / secrets — never commit
.env
.env.local
*.local

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs from local launchd schedule
tools/scheduling/refresh.log
tools/scheduling/refresh.error.log
